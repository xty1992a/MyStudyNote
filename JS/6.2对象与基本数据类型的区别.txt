基本数据类型和引用数据类型

	内存分为栈内存及堆内存.
	变量与它的值都保存在栈内存中,当比较两个变量时,会对它们的值进行相等运算.
	而对象保存在堆内存中,与它对应的变量保存的值是对对象的引用(内存地址).
	-因此,当比较两个保存对象的变量时,比较的是它们的地址,如果他们指向的对象相同,则这两个变量相等,同时,通过一个变量修改对象之后,另一个变量也会受到影响.
	-但如果一个变量修改自己的值(内存地址)时,不会影响另一个变量.
	
	-如果两个变量指向的对象不同,即使对象的内容相同,这两个变量也不会相等,他们是两个完全不同的个体.