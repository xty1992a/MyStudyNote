包装类对象
	一般而言,只有对象可以保存属性和方法,基本数据类型不能,但如果我们调用基本数据类型的方法或属性,JS并不会向我们报错.因为它会临时将这个变量转换为一个对象,操作完成后再将其变回基本数据类型.
	
	在JS中,提供了三个包装类String Boolean Number
	使用这三个包装类可以将一个基本数据类型转换为一个对象
	
	String
	-它的属性length可以获取字符串的长度.