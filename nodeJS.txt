node.js
	node.js是一种单线程,事件驱动,非阻塞I/O的js运行环境.其机制采用单线程分发大量并发请求,当请求需要I/O操作时,node.js会将其重新排进等待队列,处理下一个请求,当请求完成I/O操作时,事件触发,node.js将会继续执行该请求.
	node.js擅长处理计算量小的大量并发请求,如果请求很少,计算量又大,不建议使用node.js.
	另外,由于是单线程,node.js不够健壮,主线程崩溃将导致整个服务崩溃.
	
	